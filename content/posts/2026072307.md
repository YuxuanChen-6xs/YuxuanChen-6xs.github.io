---author: "Michael Henderson"
date: 2026-07-23
linktitle: "5分钟搞定！文心一言统一接入教程：一次接入，同时调用GPT-4、Claude、文心一言等12大模型"
menu:
  main:
    parent: tutorials
next: /posts/qianjuai-1
prev: /posts/qianjuai-2
title: "5分钟搞定！文心一言统一接入教程：一次接入，同时调用GPT-4、Claude、文心一言等12大模型"
weight: 1
tags: ["ChatGPT", "Claude", "AI中转站"]
---

# 5分钟搞定！文心一言统一接入教程：一次接入，同时调用GPT-4、Claude、文心一言等12大模型

程序员和AI产品经理们，你们是不是也经常遇到这种场景：

想测试一下GPT-4的推理能力，又想去对比下Claude的长文本理解，还得兼顾国产大模型（比如文心一言、通义千问）的合规性。为了跑通这几种API，你得在官网之间反复横跳，注册一堆账号，研究各自文档里的认证方式。一个下午过去了，还没开始调试代码，光配置环境就把精力耗没了。

**最头疼的是什么？是“一家一个接口”。**

OpenAI是OpenAI的格式，文心一言是百度自己的模式，Claude又一套东西。为了兼容它们，你往往需要在代码里写好几种不同的封装。开发效率？不存在的。

我有办法能让你彻底告别这种混乱。

👉 [注册千聚ai大模型聚合站，立即体验一站式API接入](https://www.qianjuai.com/register)



## 你能从这篇文章里获得什么？

这篇文章不是给你讲理论，而是给你一套**可以直接复制的代码实操方案**。5分钟内，我会带你完成下述动作：

1.  注册一个“中间件”平台（[千聚ai大模型聚合站](https://www.qianjuai.com/)）。
2.  获取你专属的统一 API Key。
3.  修改 1 行代码，打通 OpenAI GPT-4、Anthropic Claude、文心一言、通义千问等 12 + 主流大模型。

这件事的本质，就是**通过“统一接入层”，把所有模型的 API 调用接口强制规范成你最熟悉的那一个——OpenAI 格式。**

从此以后，你只需要维护一套调用逻辑，剩下的，交给这个聚合站去处理。

---

## 第一步：到底什么是“文心一言统一接入”？

很多朋友的误区是：认为“统一接入”就是一个网站让你填不同的 API Key，然后做个网页界面让你切换。不是的。

**真正的统一接入，是在API层面完成格式转换。**

你可以这样理解：

以前你家里有很多电器（各种大模型），每个电器都有自己独特的插头（API格式）。你需要为每个电器准备一个专门插座，才能通电工作。

现在[千聚ai大模型聚合站](https://www.qianjuai.com/)，相当于提供了一个万能的**转换插头**。

无论这个模型用的是百度 WX SDK，是 OpenAI 标准，还是 Anathropic 的消息格式，通过这个转换插头，你家里的**标准两孔插座（OpenAI兼容接口）** 就能给他们全部供电。

也就是说，你用 `openai` 这个 `Python` 库，把 `base_url` 一改，就能去调用文心一言。代码不需要任何额外逻辑。

---

## 第二步：手把手教程——5分钟接入12个模型

整个过程不需要你拥有任何翻墙环境，不需要海外信用卡。

### 第1分钟：注册并获取“万能钥匙”

1.  打开 [千聚ai大模型聚合站](www.qianjuai.com) 官网。
2.  点击右上角“注册”按钮，填写你的手机号或邮箱。
3.  登录后，进入左侧菜单栏的“API密钥”页面。
4.  点击“创建新密钥”，系统会生成一串以 `sk-` 开头的字符串。这就是你的“万能钥匙”。

> **注意：** [千聚ai大模型聚合站](https://www.qianjuai.com/)不要求你提供自己注册的其他大模型API秘钥。你在平台后台充值后，直接用平台的密钥就能调用后台所有的模型。相当于集成了各种模型的“池子”。

### 第2分钟：设置你的“标准插座”

在你现有的任何一个 Python 项目中，找到调用 OpenAI 或者任何其他大模型的地方，通常会是这个样子：

python
from openai import OpenAI

# 第一步：替换客户端地址
client = OpenAI(
    api_key = "sk-你的千聚API秘钥",   # 最关键的一步：替换为你刚创建的Key
    base_url = "https://www.qianjuai.com/v1",   # 这个地址被称为“沉浸式中转站”
)

# 第二步：发起调用——你想用哪个模型，直接换名字！
response = client.chat.completions.create(
    model = "gpt-4",   # 这里可以换成 claude-3-5-sonnet, baidu/ernie-4.0, qwen-turbo-latest 等等
    messages = [
        {"role": "system", "content": "你是一个知识渊博的学者"},
        {"role": "user", "content": "请用哲学的角度，解释一下为什么代码跑得通是玄学？"},
    ],
)

print(response.choices[0].message.content)


**看出来了吗？改动点只有一个：`base_url` 和 `api_key`。**

### 第3分钟：明确模型映射规则

你可能会觉得，“我改了个地址就行？那怎么指定调的是文心一言还是Claude？”

答案是通过 `model` 参数。

第三方聚合站会提供一份 [模型映射表](https://www.qianjuai.com/register)。当你填写 `model="baidu/ernie-4.0"` 时，平台自动将其翻译成百度的内部模型 ID 和 API 调用格式。

这里给你一个常见的“暗号”清单，供你快速上手：

*   **GPT 系列（不变）**：`gpt-4`, `gpt-4o`, `gpt-4o-mini`
*   **Claude 系列**：`claude-3-5-sonnet`, `claude-3-opus`, `claude-haiku`
*   **国产模型系列**：
    *   文心一言：`baidu/ernie-4.0`, `baidu/ernie-3.5`
    *   通义千问：`qwen-turbo-latest`, `qwen-plus-latest`, `qwen-max-latest`
    *   DeepSeek：`deepseek-chat`, `deepseek-reasoner`
    *   智谱 GLM：`glm-4`, `glm-4-plus`, `glm-4v`

> **小提示**：国外模型无限流，调用延迟低；国产模型价格极低，多用于合规验证或推理。你现在在同一份代码里，只要改 `model` 名字，就能一键切换这些模型。

### 第4分钟：处理流式输出和多模态

不只是简单的问答，流式输出也能完美支持。这是实现打字机效果的灵魂：

python
# 流式调用代码几乎和官方一模一样
stream = client.chat.completions.create(
    model = "gpt-4o",   # 也可以换成 “baidu/ernie-4.0”
    messages = [...],
    stream = True,   # 开启流式
)

for chunk in stream:
    if chunk.choices[0].delta.content is not None:
        print(chunk.choices[0].delta.content, end="")


想发图片给视觉模型（比如 GPT-4o 或通义千问VL）？

直接把图片作为 Base64 格式放进去就好，聚合站后台会自动分类处理：

python
response = client.chat.completions.create(
    model = "gpt-4o",   # 支持多模态的模型
    messages = [
        {
            "role": "user",
            "content": [
                {"type": "text", "text": "这张图里有什么？"},
                {"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,<你的图片base64编码>"}}
            ]
        }
    ],
)


---

## 第三步：解锁更多高阶玩法（集成ChatGPT类工具）

这种方式的妙处在于，**不仅仅你用代码调，还能集成到很多开源的ChatGPT前端里面。**

想象一下，你在 LobeChat、ChatGPT-Next-Web 或者沉浸式翻译这类工具里，目前只能配置一个 OpenAI Key，但现在，你可以：

1.  打开这些工具的**自定义API地址**设置。
2.  填入 `https://www.qianjuai.com/v1`。
3.  填入你在[千聚ai大模型聚合站](https://www.qianjuai.com/)生成的API Key。

**神奇的事情发生了：**

你的 LobeChat 不再只能使用 GPT。它变成了一个“超级中枢”。你可以在聊天的实时设置里，把模型从 `gpt-4` 改成 `baidu/ernie` 甚至 `claude-3-5-sonnet`。自己动手，实现 GPT 的“能力平权”和“方案兜底”。

---

## 第四步：新手如何免费体验和充值？

[千聚ai大模型聚合站](https://www.qianjuai.com/)有一个非常贴心的福利机制。

新用户注册后，平台会赠送 **\$0.2** 的免费体验额度。这点额度足够你跑通上述的所有示例代码，测试完 GPT-4、Claude 和文心一言等主流模型，确保你的接入流程没有 Bug。

体验好了，觉得值得投入了，你再考虑充值。最低充 1 块钱即可激活使用。

👉 [立即注册千聚ai大模型聚合站，领取新用户体验金](https://www.qianjuai.com/register)

---

## 写在最后：关于“一次接入”

什么是好的“一次接入”？

**绝对不是让你写一个冗长的 `switch-case` 语句，也不是让你部署一个 API 路由转发服务。**

好的接入，是让你把精力从“怎么连上都跑通”的苦力活中解放出来，回归到“用什么模型能产出什么好作品”的核心业务上。

[千聚ai大模型聚合站](https://www.qianjuai.com/)真正实现了一次接入：**改一行 base_url，从此开发者的世界里，GPT-4的调用方式，就变成了所有模型的调用方式。**

对内，是开发效率的指数级提升；对外，是全面拥抱 AI 资产控制力的体现。2025年，别再让自己陷入“模型海王”的困境里了。

从这里开始，一次接入，赢得时间。