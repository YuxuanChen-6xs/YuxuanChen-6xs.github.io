---author: "Michael Henderson"
date: 2026-07-26
linktitle: "国内开发者100%成功：免翻墙直取o4-mini API文档的保姆级保存教程"
menu:
  main:
    parent: tutorials
next: /posts/qianjuai-1
prev: /posts/qianjuai-2
title: "国内开发者100%成功：免翻墙直取o4-mini API文档的保姆级保存教程"
weight: 1
tags: ["API接口", "ChatGPT"]
---

# 国内开发者100%成功：免翻墙直取o4-mini API文档的保姆级保存教程

说实话，又有一波开发者被“o4-mini”这个新模型给撩到了，但一看官方文档，心里直接凉了半截——又要科学上网，又要翻墙看官网，一不小心连API Key都申请不下来。折腾半天，代码一行没写，脑子先炸了。

这事我熟，也是从那个阶段走过来的。后来好说歹说，找到了一个不折腾的办法，不仅把o4-mini API文档完整拿下来了，还顺手跑通了代码。今天这文章，就是手把手告诉你：怎么在国内网络下，100%成功、无痛地拿到o4-mini API文档，并且保存得稳稳当当的。

---

👉 [立即注册千聚api聚合站，新用户送 \$0.2 消费额度](https://www.qianjuai.com/register)

---

## 核心思路：借一座“桥”，一步到位

先别急着问我“桥”是什么，你只需要知道一件事：

> **国内网络直连o4-mini API，不需要代理，不需要海外信用卡，不需要翻墙。**

怎么做到的呢？只需一个聚合平台——[千聚api聚合站](https://www.qianjuai.com/)（www.qianjuai.com）。它提供的API接口，完全兼容OpenAI标准，你把原来的 `base_url` 换一下就搞定了。

你是不是在想：就这么简单？

对，就这么简单。我不卖关子，下面直接上教程。

---

## 保姆级教程：3步搞定，零门槛

### 第一步：注册[千聚api聚合站](https://www.qianjuai.com/)账号

打开 [注册链接](https://www.qianjuai.com/register) ，填个邮箱，设个密码，两分钟完事。

关键点：**注册成功即送 \$0.2 额度**，一分钱不用充，直接就能用来测试。

---

### 第二步：创建并获取你的API Key

注册后登录【[千聚api聚合站](https://www.qianjuai.com/)】，找到“API Key 管理”或者“令牌管理”入口。点击“创建Key”，选一个你自己记得住的名字，比如“test-o4-mini”。

创建完之后，它会给出一串像这样的字符：


sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx


这串字符就是你的通行证，**复制下来，拿小本本记好**。

---

### 第三步：修改代码中的base_url，直取API文档

现在，你不需要再去看o4-mini的官方文档怎么找，也不用关心它原生的接口是什么了。因为[千聚api聚合站](https://www.qianjuai.com/)已经帮你做完了所有适配。

你只需要把你代码里原来的 `base_url` 从：

python
# 原来
base_url = "https://api.openai.com/v1"


换成：

python
# 换成千聚的
base_url = "https://www.qianjuai.com/v1"


然后，在你原有的调用代码里，把API Key替换成你第二步拿到的key，模型名称换成 `gpt-4o-mini` 或者千聚支持的o4系列模型名称（具体名称见后台模型列表）。

**举个例子**，如果你的代码原来调用GPT-4o的，现在直接换一行地址，模型名改一下，就能调用o4-mini了，**完整文档不需要你单独去找**，因为千聚的接口完全对标官方，你可以直接套用官方文档里的任何代码示例。

---

👉 [注册千聚api聚合站，体验无痛调用大模型](https://www.qianjuai.com/register)

---

## 进阶操作：如何“保存”整份文档

有些同学说，我不只是想调用API，我还想把整份o4-mini API文档保存到本地，离线查看。

好，我给你三个方法：

**方法一：开发者工具“另存为”**
在浏览器的地址栏输入 `https://platform.openai.com/docs/` （假设你能访问），按 `Ctrl+S` 或者 `Cmd+S`，选择保存类型为“网页，全部”，就可以把当前页面的HTML版本下载下来。

**方法二：curl 命令批量抓取**
如果你有命令行基础，在终端里执行：

bash
curl -H "Authorization: Bearer sk-your-api-key" \
https://www.qianjuai.com/v1/models


这个命令会返回千聚支持的模型列表，其中就包含o4-mini及其模型ID。然后把API文档对应的路由（千聚页面或官方文档页面）进行抓取，使用 `wget` 或 `curl -o filename.html` 保存到本地。

**方法三：使用浏览器插件**
安装“SingleFile”或“Save Page WE”这类插件，一键保存完整页面到本地。离线后照样看，比收藏夹还稳。

---

## 警惕三个常见坑

我踩过的雷，不希望你再踩一遍：

**坑一：API Key填错了位置**
有些开发者把Key填在了 `base_url` 后面，或者没复制完整。**记住，Key是放在 `Authorization` 头里的，不是URL的一部分**。

**坑二：模型名称写错**
不要写 `o4-mini`，也不一定写 `gpt-4o-mini`，具体名称请登录千聚后台，在模型列表里找到它给的准名，复制粘贴最安全。

**坑三：以为必须翻墙**
真的不需要。[千聚api聚合站](https://www.qianjuai.com/)的服务器在国外有节点，走的是国内直连，你不用挂任何代理，直接请求就行了。

---

## 代码运行效果实测

给你看一段最简单的测试代码（Python）：

python
from openai import OpenAI

client = OpenAI(
    base_url="https://www.qianjuai.com/v1",
    api_key="sk-你的key"
)

response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": "你好，介绍一下o4-mini的特点"}]
)

print(response.choices[0].message.content)


执行后，只要网络是通的，大约1-3秒就返回结果。没翻墙、没绑卡、没注册海外账号。真就一行地址的差距。

---

## 支持哪些平台和工具

千聚这个接口不挑客户端，几乎所有主流的AI客户端都能接：

- Cursor （写代码的IDE）
- Cline （VS Code插件）
- LobeChat （聊天机器人）
- ChatGPT Next Web （网页版ChatGPT）
- Cherry Studio （桌面端AI合集）
- 沉浸式翻译 （浏览器翻译插件）
- 甚至你自己的Python脚本、LangChain项目

只要是能自定义 `base_url` 的工具，都能把原来指向OpenAI的地址，改成千聚的地址，立马就能用o4-mini。

---

## 新用户福利：先白嫖，再决定

[千聚api聚合站](https://www.qianjuai.com/)对新用户相当友好：

1. 注册就送 **\$0.2 余额**，不用充值就能跑通上面这段测试代码。
2. 每天还有免费额度（需要去免费子站 `free.yunwu.ai` 用GitHub登录看看，有GPT-4o和GPT-4o-mini的免费调用）。
3. 觉得好用，最低充1块钱（等于1美元Token额度）就能继续用。

说实话，我试过十几个中转站，能免费试到这种程度的，真不多。

👉 [注册千聚api聚合站，免费领额度试试水](https://www.qianjuai.com/register)

---

## 一句话总结

国内开发者想要免翻墙拿到o4-mini API文档并顺利使用，**核心套路只有三个字：接中转。**

具体来说：注册[千聚api聚合站](https://www.qianjuai.com/) -> 换 `base_url` -> 换Key -> 调用完成。

剩下的文档保存、批量请求、离线查看，都是锦上添花。

这个方法我已经跑了两个月，稳得一批。如果你还在为翻墙和绑卡头疼，不如花三分钟注册一下，把代码跑通，省下来的精力，用来做更有价值的事情。